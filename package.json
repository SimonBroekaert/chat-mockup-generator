{
    "name": "chat-mockup-generator",
    "version": "2.0.0",
    "private": true,
    "description": "A browser-based chat mockup generator. TypeScript sources and a Tailwind stylesheet, zero runtime dependencies; served and built with Bun.",
    "type": "module",
    "scripts": {
        "build:css": "tailwindcss -i ./src/input.css -o ./app.css",
        "watch:css": "tailwindcss -i ./src/input.css -o ./app.css --watch",
        "serve": "bun run build:css && bun serve.ts",
        "build": "bun run build:css && bun build index.html --outdir dist",
        "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.tools.json",
        "lint": "bun scripts/check-types.ts",
        "check": "bun run typecheck && bun run lint && bun test",
        "test": "bun test"
    },
    "devDependencies": {
        "@tailwindcss/cli": "^4.3.3",
        "@types/bun": "^1.3.14",
        "tailwindcss": "^4.3.3",
        "typescript": "^5.9.3"
    }
}
