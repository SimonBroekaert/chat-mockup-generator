{
    "name": "chat-mockup-generator",
    "version": "2.0.0",
    "private": true,
    "description": "A browser-based chat mockup generator. Static files, no build step, no runtime dependencies.",
    "type": "module",
    "scripts": {
        "serve": "python3 -m http.server 8765",
        "test": "node --test"
    }
}
